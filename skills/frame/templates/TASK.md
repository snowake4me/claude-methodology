# Task: [Short Title]

**ID:** TASK-YYYY-MM-DD-slug
**Scope:** feature | bug fix | enhancement | refactor
**Touches:** `path/to/file1`, `path/to/file2`

---

## Background

1–3 sentences. Why is this needed? What breaks or is missing today? Reference
prior work or a parent spec if relevant (e.g. "from SPEC-YYYY-MM-DD-slug").

## Changes

The *what*, from the caller's / user's point of view — not the *how* (that goes
in Implementation notes).

### 1. [Change name]

What it does. Optional before/after or usage example.

### 2. [Change name]

...

## Acceptance criteria

- [ ] Specific, verifiable outcome (prefer "doing X with Y yields Z")
- [ ] Error / edge cases handled (e.g. "fails with a clear message when …")
- [ ] Existing behavior unchanged where it should be
- [ ] Verified: <how — run the app, a test, manual eyeball>

## Implementation notes

Hints for the implementer — non-obvious details, gotchas, preferred patterns,
where the entry point lives. Skip anything self-evident from the code.

## Out of scope

Related things explicitly NOT in this task (prevents scope creep). Delete if none.

## Release impact

If the project versions releases: bump tier + which files to update (CHANGELOG,
release notes) + the deploy target. Otherwise "n/a".

---
**Created:** YYYY-MM-DD
